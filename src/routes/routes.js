const BASIC_SLASH = "/";
const API_ROUTE = "/api";

// TEST

const TEST_ROUTE = "/test";

const ROUTES = {
  basicSlash: BASIC_SLASH,
  apiRoute: API_ROUTE,
  testRoute: TEST_ROUTE
};

export default ROUTES;
